<section>
<p>
      vMembreAjouter
      </p>
  <h1>{{ titre|upper }}</h1>
  
  <form method="POST" action="creer?entite=utilisateur&action=a">

    <label>Nom</label>
    <input name="utilisateur_nom" value="{{ utilisateur.utilisateur_nom }}">
    <span>&nbsp;{{ erreurs.utilisateur_nom }}</span>

    <label>Prénom</label>
    <input name="utilisateur_prenom" value="{{ utilisateur.utilisateur_prenom }}">
    <span>&nbsp;{{ erreurs.utilisateur_prenom }}</span>

    <label>Courriel</label>
    <input name="utilisateur_courriel" value="{{ utilisateur.utilisateur_courriel }}">
    <span>&nbsp;{{ errCourriel }} {{ erreurs.utilisateur_courriel }}</span>

   <label>Mdp</label>
    <input name="utilisateur_mdp" value="{{ utilisateur.utilisateur_mdp }}">
    <span>&nbsp;{{ errCourriel }} {{ erreurs.utilisateur_mdp }}</span>

    <label>Profil</label>
    <select name="utilisateur_profil">
      
      <option value="{{ constant('utilisateur::PROFIL_MEMBRE') }}" {{ utilisateur.utilisateur_profil == constant('utilisateur::PROFIL_MEMBRE')    ? 'selected' : '' }}>Membre</option>
      
    </select>
    <span>&nbsp;{{ erreurs.utilisateur_profil }}</span>

  
    <input type="submit" value="Envoyer">
</form>
  </div>
</section>